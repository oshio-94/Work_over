#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -Entry point
 *
 * Return:0
 */
int main(void)
{
        for (int a = 0; a <= 10; a++)
        {
                for (char b = ' '; a <= 10; a++)
                {
                        for (int c = 0; c <= 10; c++)
                        {
					       putchar(a);
					       putchar(b);
					       putchar(':'); * 2;
					       putchar(c);
					       putchar(d);
					       putchar(' ');
                        }
                }
        }
        return (0);
}
